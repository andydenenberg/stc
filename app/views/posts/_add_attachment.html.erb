<%= simple_form_for(@attachment, :html => { :class => 'form-horizontal blank_box', :multipart => true } ) do |f| %>

<%= f.hidden_field :post_id , :value => @post.id %>

<% if @attach_url == 'url' %>
Add URL:<br>    
<%= f.text_field :url, :placeholder => 'www.example.com' %><br>
<br>    
<%= f.button :submit , 'Create Link', :class => 'btn btn-small' %>
<% elsif @attach_url == 'image' %>
Choose image file, then upload:<br><br>
<%= f.file_field :image %><br>
<br>    
<%= f.button :submit , 'Upload Image', :class => 'btn btn-small' %>
<% elsif @attach_url == 'file' %>
Choose file, then upload it:<br><br>
<%= f.file_field :image %><br>
<br>    
<%= f.button :submit , 'Upload File', :class => 'btn btn-small' %>
<% end %>
<%= link_to 'Cancel' , :back %>



<% end %>
